@model HomePageViewModel

<section class="mb-16">
    <h2 class="text-4xl font-bold mb-8 text-gray-900 dark:text-white">Season Leaders</h2>

    <div class="bg-white dark:bg-gray-800 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700 shadow-sm">
        <!-- Tab Headers -->
        <div class="flex border-b border-gray-200 dark:border-gray-700">
            <button class="leaders-tab flex-1 py-4 px-6 font-bold text-lg transition-all flex items-center justify-center gap-3" data-tab="indoor">
                🏢 INDOOR
            </button>
            <button class="leaders-tab flex-1 py-4 px-6 font-bold text-lg transition-all flex items-center justify-center gap-3" data-tab="outdoor">
                ☀️ OUTDOOR
            </button>
        </div>

        <!-- Outdoor Content -->
        <div id="leaders-outdoor" class="leaders-content p-8">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                <!-- Boys Leaders -->
                <div class="bg-gray-50 dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden shadow-sm">
                    <div class="bg-blue-50 dark:bg-blue-900/30 px-6 py-4 border-b border-blue-200 dark:border-blue-700/30">
                        <h3 class="text-2xl font-bold text-blue-900 dark:text-blue-300">Boys Top Marks</h3>
                    </div>
                    <div class="divide-y divide-gray-200 dark:divide-gray-700">
                        @if (Model.BoysOutdoorLeaders.Any())
                        {
                            @foreach (var leader in Model.BoysOutdoorLeaders)
                            {
                                <div class="px-6 py-5 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">
                                    <div class="flex justify-between items-start gap-4">
                                        <div class="flex-1 min-w-0">
                                            <div class="font-semibold text-lg text-gray-900 dark:text-white mb-1">@leader.EventName</div>
                                            <div class="text-sm text-gray-600 dark:text-gray-400">
                                                <a asp-controller="Roster" asp-action="Details" asp-route-slug="@leader.AthleteSlug" class="underline">
                                                    @leader.AthleteFullName
                                                </a>
                                            </div>
                                        </div>
                                        <div class="text-right flex-shrink-0">
                                            <div class="font-mono font-bold text-blue-600 dark:text-blue-400 text-xl mb-1">@leader.Performance</div>
                                            @if (leader.AllTimeRank.HasValue)
                                            {
                                                <div class="text-xs text-gray-500 dark:text-gray-500 font-medium">
                                                    #@leader.AllTimeRank All-Time
                                                    @if (leader.AllTimeRank == 1)
                                                    {
                                                        <span class="ml-1">🏆</span>
                                                    }
                                                </div>
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="px-6 py-8 text-center text-gray-500 dark:text-gray-400">
                                No performances yet this season
                            </div>
                        }
                    </div>
                </div>

                <!-- Girls Leaders -->
                <div class="bg-gray-50 dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden shadow-sm">
                    <div class="bg-pink-50 dark:bg-pink-900/30 px-6 py-4 border-b border-pink-200 dark:border-pink-700/30">
                        <h3 class="text-2xl font-bold text-pink-900 dark:text-pink-300">Girls Top Marks</h3>
                    </div>
                    <div class="divide-y divide-gray-200 dark:divide-gray-700">
                        @if (Model.GirlsOutdoorLeaders.Any())
                        {
                            @foreach (var leader in Model.GirlsOutdoorLeaders)
                            {
                                <div class="px-6 py-5 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">
                                    <div class="flex justify-between items-start gap-4">
                                        <div class="flex-1 min-w-0">
                                            <div class="font-semibold text-lg text-gray-900 dark:text-white mb-1">@leader.EventName</div>
                                            <div class="text-sm text-gray-600 dark:text-gray-400">
                                                <a asp-controller="Roster" asp-action="Details" asp-route-slug="@leader.AthleteSlug" class="underline">
                                                    @leader.AthleteFullName
                                                </a>
                                            </div>
                                        </div>
                                        <div class="text-right flex-shrink-0">
                                            <div class="font-mono font-bold text-pink-600 dark:text-pink-400 text-xl mb-1">@leader.Performance</div>
                                            @if (leader.AllTimeRank.HasValue)
                                            {
                                                <div class="text-xs text-gray-500 dark:text-gray-500 font-medium">
                                                    #@leader.AllTimeRank All-Time
                                                    @if (leader.AllTimeRank == 1)
                                                    {
                                                        <span class="ml-1">🏆</span>
                                                    }
                                                </div>
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="px-6 py-8 text-center text-gray-500 dark:text-gray-400">
                                No performances yet this season
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>

        <!-- Indoor Content -->
        <div id="leaders-indoor" class="leaders-content hidden p-8">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                <!-- Boys Leaders -->
                <div class="bg-gray-50 dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden shadow-sm">
                    <div class="bg-blue-50 dark:bg-blue-900/30 px-6 py-4 border-b border-blue-200 dark:border-blue-700/30">
                        <h3 class="text-2xl font-bold text-blue-900 dark:text-blue-300">Boys Top Marks</h3>
                    </div>
                    <div class="divide-y divide-gray-200 dark:divide-gray-700">
                        @if (Model.BoysIndoorLeaders.Any())
                        {
                            @foreach (var leader in Model.BoysIndoorLeaders)
                            {
                                <div class="px-6 py-5 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">
                                    <div class="flex justify-between items-start gap-4">
                                        <div class="flex-1 min-w-0">
                                            <div class="font-semibold text-lg text-gray-900 dark:text-white mb-1">@leader.EventName</div>
                                            <div class="text-sm text-gray-600 dark:text-gray-400">
                                                <a asp-controller="Roster" asp-action="Details" asp-route-slug="@leader.AthleteSlug" class="underline">
                                                    @leader.AthleteFullName
                                                </a>
                                            </div>
                                        </div>
                                        <div class="text-right flex-shrink-0">
                                            <div class="font-mono font-bold text-blue-600 dark:text-blue-400 text-xl mb-1">@leader.Performance</div>
                                            @if (leader.AllTimeRank.HasValue)
                                            {
                                                <div class="text-xs text-gray-500 dark:text-gray-500 font-medium">
                                                    #@leader.AllTimeRank All-Time
                                                    @if (leader.AllTimeRank == 1)
                                                    {
                                                        <span class="ml-1">🏆</span>
                                                    }
                                                </div>
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="px-6 py-8 text-center text-gray-500 dark:text-gray-400">
                                No performances yet this season
                            </div>
                        }
                    </div>
                </div>

                <!-- Girls Leaders -->
                <div class="bg-gray-50 dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden shadow-sm">
                    <div class="bg-pink-50 dark:bg-pink-900/30 px-6 py-4 border-b border-pink-200 dark:border-pink-700/30">
                        <h3 class="text-2xl font-bold text-pink-900 dark:text-pink-300">Girls Top Marks</h3>
                    </div>
                    <div class="divide-y divide-gray-200 dark:divide-gray-700">
                        @if (Model.GirlsIndoorLeaders.Any())
                        {
                            @foreach (var leader in Model.GirlsIndoorLeaders)
                            {
                                <div class="px-6 py-5 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">
                                    <div class="flex justify-between items-start gap-4">
                                        <div class="flex-1 min-w-0">
                                            <div class="font-semibold text-lg text-gray-900 dark:text-white mb-1">@leader.EventName</div>
                                            <div class="text-sm text-gray-600 dark:text-gray-400">
                                                <a asp-controller="Roster" asp-action="Details" asp-route-slug="@leader.AthleteSlug" class="underline">
                                                    @leader.AthleteFullName
                                                </a>
                                            </div>
                                        </div>
                                        <div class="text-right flex-shrink-0">
                                            <div class="font-mono font-bold text-pink-600 dark:text-pink-400 text-xl mb-1">@leader.Performance</div>
                                            @if (leader.AllTimeRank.HasValue)
                                            {
                                                <div class="text-xs text-gray-500 dark:text-gray-500 font-medium">
                                                    #@leader.AllTimeRank All-Time
                                                    @if (leader.AllTimeRank == 1)
                                                    {
                                                        <span class="ml-1">🏆</span>
                                                    }
                                                </div>
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="px-6 py-8 text-center text-gray-500 dark:text-gray-400">
                                No performances yet this season
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>