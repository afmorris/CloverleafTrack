@model List<SeasonCardViewModel>

<section>
    <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">Seasons</h2>
    @foreach (var season in Model)
    {
        @await Html.PartialAsync("_SeasonCard", season)
    }
</section>